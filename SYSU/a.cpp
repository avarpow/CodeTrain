#include <iostream>
#include <string.h>
#include <stdio.h>
#include <vector>
#include <queue>
#include <set>
#include <string>
#include <algorithm>
#include <math.h>
#define Please return
#define AC 0
// #define yn
using namespace std;
void solve()
{
    int n, flag = 0;
    cin >> n;

#ifdef yn
    if (flag)
    {
        cout << "YES" << endl;
    }
    else
    {
        cout << "NO" << endl;
    }
#endif
}
int main()
{
    double x, y, a, b;
    cin >> x >> y >> a >> b;
    double dx = a / (2.00 + sqrt(2.00));
    printf("%.10lf", x + dx);
    Please AC;
}